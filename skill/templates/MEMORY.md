# 记忆索引

> 仅存放指针 —— 实际内容永远保存在链接到的文件中，不要写在这里。
> 软限制 150 行 / 20 KB（警告）。硬限制 200 行 / 25 KB（先压缩）。
> 每条记忆一行：`- [<标题>](<slug>.md) — <一句话钩子>`
> 纯文本且未加密：永远不要在这里存储机密 *值*（只写它存在的位置）；
> 保持此存储库被 git 忽略，并远离云同步 / 备份文件夹。
<!-- 可选 —— 如果你不维护知识库，请删除本行（SKILL.md §6.1）。
     将其指向你的知识库；文章是供人类阅读的，不会在这里建立索引。
> 📚 知识库 `<path>/` —— 为人类而写，不是记忆；其文章不会被索引到这里。
> 弄清了某个机制 / 排除了一个错误假设 / 写下了一篇长说明 → 阅读 `<path>/README.md`
> 并决定是否 PROPOSE 一篇文章（只提出建议，不要未经请求就撰写）。
-->

## user
<!-- 用户是谁：身份、角色、专长、关于他们的持久偏好。
     关于“你如何工作”的偏好（例如“用中文回复”）属于 feedback，不是 user。 -->

## feedback
<!-- 仅存放可复用的纠正 / 工作流（不是任务本地的状态）。Why: / How to apply: -->

## project
<!-- 用于恢复工作所需的目标 / 状态 / 决策 / 约束 / 阻塞 / 下一步。
     重新验证代码 / git / 测试指针；归档已完成的瞬时状态。
     使用绝对日期。Why: / How to apply: -->

## reference
<!-- 指向外部资源的指针：URL、仪表盘、工单、日志路径 -->
